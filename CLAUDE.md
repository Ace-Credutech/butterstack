# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current State

This repository is in early initialization. The only committed file is `.vscode/settings.json`, which configures VS Code workspace colors (green theme via Peacock).

No source code, build system, or framework has been set up yet.

## VS Code Configuration

The workspace uses a green color theme (`#215732`) set via the Peacock extension, applied to the activity bar, status bar, and title bar.
